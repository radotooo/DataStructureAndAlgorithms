function search(arr,left,right,num){
    if(!arr){
        return "no item"
    }
    const midIndex = arr/2
    if(arr[midIndex] === num){
        return arr[midIndex]
    }
    const leftPartt = arr.s
    if(arr[midIndex] < num){
        search(arr,num)
    }
    if(arr[midIndex] > num){
        search
    }
}

